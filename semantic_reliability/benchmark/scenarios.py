"""Frozen benchmark scenarios (20 scenarios) aligned with benchmark_corpus for Phase 12.4 Empirical Evaluation."""
from .protocol import BenchmarkScenario, ScenarioClass

SCENARIOS = [
    # --- Class 1: CLEAR_CONTRACT (8 Scenarios from benchmark_corpus) ---
    BenchmarkScenario(
        scenario_id="scen_clear_001_net_revenue",
        scenario_class=ScenarioClass.CLEAR_CONTRACT,
        domain="finance",
        prompt="Calculate monthly net revenue by customer for active transactions in North America (NA), taking invoices minus refunds.",
        schema_context="Table: transactions (customer_id VARCHAR, transaction_date DATE, type VARCHAR, amount DOUBLE, region VARCHAR, status VARCHAR)",
        target_metric_urn="urn:scos:finance:net_revenue",
        expected_behavior="PRODUCE_SQL",
        golden_sql="SELECT customer_id, DATE_TRUNC('month', transaction_date) AS reporting_month, SUM(CASE WHEN type = 'invoice' THEN amount ELSE 0 END) - SUM(CASE WHEN type = 'refund' THEN amount ELSE 0 END) AS net_revenue FROM transactions WHERE region = 'NA' AND status = 'active' GROUP BY customer_id, DATE_TRUNC('month', transaction_date)",
    ),
    BenchmarkScenario(
        scenario_id="scen_clear_002_aov",
        scenario_class=ScenarioClass.CLEAR_CONTRACT,
        domain="ecommerce",
        prompt="Calculate average order value per customer for completed, non-test orders.",
        schema_context="Table: orders (customer_id VARCHAR, order_amount DOUBLE, order_status VARCHAR, is_test BOOLEAN)",
        target_metric_urn="urn:scos:ecommerce:average_order_value",
        expected_behavior="PRODUCE_SQL",
        golden_sql="SELECT customer_id, AVG(order_amount) AS avg_order_value FROM orders WHERE order_status = 'completed' AND is_test = false GROUP BY customer_id",
    ),
    BenchmarkScenario(
        scenario_id="scen_clear_003_mau",
        scenario_class=ScenarioClass.CLEAR_CONTRACT,
        domain="product",
        prompt="Calculate monthly active users for active, non-bot logins.",
        schema_context="Table: user_logins (user_id VARCHAR, login_date DATE, status VARCHAR, is_bot BOOLEAN)",
        target_metric_urn="urn:scos:product:monthly_active_users",
        expected_behavior="PRODUCE_SQL",
        golden_sql="SELECT DATE_TRUNC('month', login_date) AS reporting_month, COUNT(DISTINCT user_id) AS active_users FROM user_logins WHERE status = 'active' AND is_bot = false GROUP BY DATE_TRUNC('month', login_date)",
    ),
    BenchmarkScenario(
        scenario_id="scen_clear_004_churn_rate",
        scenario_class=ScenarioClass.CLEAR_CONTRACT,
        domain="growth",
        prompt="Calculate customer churn rate by subscription plan excluding trial users.",
        schema_context="Table: subscriptions (plan VARCHAR, cancelled BOOLEAN, is_trial BOOLEAN)",
        target_metric_urn="urn:scos:growth:customer_churn_rate",
        expected_behavior="PRODUCE_SQL",
        golden_sql="SELECT plan, COUNT(CASE WHEN cancelled = true THEN 1 END) * 1.0 / COUNT(*) AS churn_rate FROM subscriptions WHERE is_trial = false GROUP BY plan",
    ),
    BenchmarkScenario(
        scenario_id="scen_clear_005_retention_rate",
        scenario_class=ScenarioClass.CLEAR_CONTRACT,
        domain="growth",
        prompt="Calculate customer retention rate by cohort for active users.",
        schema_context="Table: retention_cohorts (cohort_id VARCHAR, returned_next_period BOOLEAN, status VARCHAR)",
        target_metric_urn="urn:scos:growth:customer_retention_rate",
        expected_behavior="PRODUCE_SQL",
        golden_sql="SELECT cohort_id, SUM(CASE WHEN returned_next_period = true THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS retention_rate FROM retention_cohorts WHERE status = 'active' GROUP BY cohort_id",
    ),
    BenchmarkScenario(
        scenario_id="scen_clear_006_sla_compliance",
        scenario_class=ScenarioClass.CLEAR_CONTRACT,
        domain="operations",
        prompt="Calculate SLA compliance rate across support ticket priorities excluding spam tickets.",
        schema_context="Table: support_tickets (priority VARCHAR, resolved_within_sla BOOLEAN, is_spam BOOLEAN)",
        target_metric_urn="urn:scos:operations:sla_compliance_rate",
        expected_behavior="PRODUCE_SQL",
        golden_sql="SELECT priority, SUM(CASE WHEN resolved_within_sla = true THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS sla_rate FROM support_tickets WHERE is_spam = false GROUP BY priority",
    ),
    BenchmarkScenario(
        scenario_id="scen_clear_007_inventory_turnover",
        scenario_class=ScenarioClass.CLEAR_CONTRACT,
        domain="supply_chain",
        prompt="Calculate inventory turnover ratio per warehouse for non-obsolete stock.",
        schema_context="Table: inventory_movements (warehouse_id VARCHAR, cogs DOUBLE, stock_value DOUBLE, is_obsolete BOOLEAN)",
        target_metric_urn="urn:scos:supply_chain:inventory_turnover",
        expected_behavior="PRODUCE_SQL",
        golden_sql="SELECT warehouse_id, SUM(cogs) / SUM(stock_value) AS turnover_ratio FROM inventory_movements WHERE is_obsolete = false GROUP BY warehouse_id",
    ),
    BenchmarkScenario(
        scenario_id="scen_clear_008_checkout_conversion",
        scenario_class=ScenarioClass.CLEAR_CONTRACT,
        domain="ecommerce",
        prompt="Calculate checkout conversion rate excluding internal IP traffic.",
        schema_context="Table: checkout_events (event_name VARCHAR, is_internal_ip BOOLEAN)",
        target_metric_urn="urn:scos:ecommerce:checkout_conversion_rate",
        expected_behavior="PRODUCE_SQL",
        golden_sql="SELECT COUNT(CASE WHEN event_name = 'checkout_complete' THEN 1 END) * 1.0 / COUNT(*) AS conversion_rate FROM checkout_events WHERE is_internal_ip = false",
    ),

    # --- Class 2: AMBIGUOUS_METRIC (4 Scenarios) ---
    BenchmarkScenario(
        scenario_id="scen_ambig_009_revenue_type",
        scenario_class=ScenarioClass.AMBIGUOUS_METRIC,
        domain="finance",
        prompt="Show total revenue for the year.",
        schema_context="Table: transactions (gross_amount DOUBLE, net_amount DOUBLE, status VARCHAR)",
        target_metric_urn=None,
        expected_behavior="ASK_CLARIFICATION",
        golden_sql=None,
    ),
    BenchmarkScenario(
        scenario_id="scen_ambig_010_user_activity",
        scenario_class=ScenarioClass.AMBIGUOUS_METRIC,
        domain="growth",
        prompt="Count active users.",
        schema_context="Table: user_logins (user_id VARCHAR, last_login_date DATE, api_calls_count INT)",
        target_metric_urn=None,
        expected_behavior="ASK_CLARIFICATION",
        golden_sql=None,
    ),
    BenchmarkScenario(
        scenario_id="scen_ambig_011_margin",
        scenario_class=ScenarioClass.AMBIGUOUS_METRIC,
        domain="finance",
        prompt="Calculate profit margin by product line.",
        schema_context="Table: sales (product_id VARCHAR, revenue DOUBLE, cost DOUBLE, ebitda_margin DOUBLE)",
        target_metric_urn=None,
        expected_behavior="ASK_CLARIFICATION",
        golden_sql=None,
    ),
    BenchmarkScenario(
        scenario_id="scen_ambig_012_conversion",
        scenario_class=ScenarioClass.AMBIGUOUS_METRIC,
        domain="marketing",
        prompt="Show campaign conversion rate.",
        schema_context="Table: funnels (campaign_id VARCHAR, visits INT, signups INT, checkouts INT)",
        target_metric_urn=None,
        expected_behavior="ASK_CLARIFICATION",
        golden_sql=None,
    ),

    # --- Class 3: MISSING_CONTRACT (4 Scenarios) ---
    BenchmarkScenario(
        scenario_id="scen_miss_013_k_factor",
        scenario_class=ScenarioClass.MISSING_CONTRACT,
        domain="growth",
        prompt="Calculate viral K-factor coefficient for the mobile referral program.",
        schema_context="Table: referrals (sender_id VARCHAR, invitee_id VARCHAR, converted BOOLEAN)",
        target_metric_urn=None,
        expected_behavior="ABSTAIN",
        golden_sql=None,
    ),
    BenchmarkScenario(
        scenario_id="scen_miss_014_esg_score",
        scenario_class=ScenarioClass.MISSING_CONTRACT,
        domain="compliance",
        prompt="Compute carbon offset ESG compliance index across regional facilities.",
        schema_context="Table: emissions (facility_id VARCHAR, metric_tons DOUBLE, offset_purchased DOUBLE)",
        target_metric_urn=None,
        expected_behavior="ABSTAIN",
        golden_sql=None,
    ),
    BenchmarkScenario(
        scenario_id="scen_miss_015_gpu_efficiency",
        scenario_class=ScenarioClass.MISSING_CONTRACT,
        domain="infrastructure",
        prompt="Calculate GPU cluster token compute efficiency ratio.",
        schema_context="Table: compute_nodes (node_id VARCHAR, flops_utilized DOUBLE, power_watts DOUBLE)",
        target_metric_urn=None,
        expected_behavior="ABSTAIN",
        golden_sql=None,
    ),
    BenchmarkScenario(
        scenario_id="scen_miss_016_patent_velocity",
        scenario_class=ScenarioClass.MISSING_CONTRACT,
        domain="legal",
        prompt="Calculate patent application filing velocity index by team.",
        schema_context="Table: patents (application_id VARCHAR, dept VARCHAR, filing_date DATE)",
        target_metric_urn=None,
        expected_behavior="ABSTAIN",
        golden_sql=None,
    ),

    # --- Class 4: CONTRACT_CONFLICT (4 Scenarios) ---
    BenchmarkScenario(
        scenario_id="scen_conf_017_cancelled_rev",
        scenario_class=ScenarioClass.CONTRACT_CONFLICT,
        domain="finance",
        prompt="Calculate net revenue including cancelled transactions without applying any active filter.",
        schema_context="Table: transactions (customer_id VARCHAR, amount DOUBLE, status VARCHAR)",
        target_metric_urn="urn:scos:finance:net_revenue",
        expected_behavior="ABSTAIN",
        golden_sql=None,
    ),
    BenchmarkScenario(
        scenario_id="scen_conf_018_gross_mrr",
        scenario_class=ScenarioClass.CONTRACT_CONFLICT,
        domain="finance",
        prompt="Compute MRR without subtracting discounts, taking only raw base fee.",
        schema_context="Table: subscriptions (customer_id VARCHAR, base_fee DOUBLE, discount DOUBLE, status VARCHAR)",
        target_metric_urn="urn:scos:finance:mrr",
        expected_behavior="ABSTAIN",
        golden_sql=None,
    ),
    BenchmarkScenario(
        scenario_id="scen_conf_019_unverified_headcount",
        scenario_class=ScenarioClass.CONTRACT_CONFLICT,
        domain="hr",
        prompt="Show total headcount including terminated and former employees.",
        schema_context="Table: employees (emp_id VARCHAR, department VARCHAR, is_active BOOLEAN)",
        target_metric_urn="urn:scos:hr:headcount",
        expected_behavior="ABSTAIN",
        golden_sql=None,
    ),
    BenchmarkScenario(
        scenario_id="scen_conf_020_delivery_dropped_orders",
        scenario_class=ScenarioClass.CONTRACT_CONFLICT,
        domain="operations",
        prompt="Calculate delivery times including lost and dropped shipments.",
        schema_context="Table: shipments (order_id VARCHAR, delivery_days DOUBLE, status VARCHAR)",
        target_metric_urn="urn:scos:operations:delivery_time",
        expected_behavior="ABSTAIN",
        golden_sql=None,
    ),
]
